@model IEnumerable<Sitio_Web_Core_MVC_CRUD_EF.Models.Periferico>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de perifericos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear Nuevo Periferico</a>
</p>
<table class="table table-bordered table-dark">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PerifericoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serial)
            </th>
            <th>Operaciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PerifericoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Serial)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.IdPeriferico" class="btn btn-primary">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.IdPeriferico" class="btn btn-info">Detalles</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdPeriferico" class="btn btn-danger">Borrar</a>
            </td>
        </tr>
}
    </tbody>
</table>
