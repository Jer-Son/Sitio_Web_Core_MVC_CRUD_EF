@model Sitio_Web_Core_MVC_CRUD_EF.Models.LeasingPerifericos

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>LeasingPerifericos</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PerifericoId" class="control-label"></label>
                <select asp-for="PerifericoId" class ="form-control" asp-items="ViewBag.PerifericoId"></select>
            </div>
            <div class="form-group">
                <label asp-for="LeasingId" class="control-label"></label>
                <select asp-for="LeasingId" class ="form-control" asp-items="ViewBag.LeasingId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
